# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: spring-petclinic-appservice-ai
metadata:
  template: springboot-petclinic-ai-chat-on-app-service
services:
  petclinic:
    project: .
    dist: target
    language: java
    host: appservice
